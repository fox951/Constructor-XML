
&НаКлиенте
Процедура ГиперСсылкаНажатие(Элемент, СтандартнаяОбработка)
	
	ОМ_КлиентСервер.ПерейтиПоГИперСсылке(Объект.ГиперСсылка, СтандартнаяОбработка);
	
КонецПроцедуры

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("ЭтоПомощник") Тогда
		
		ЭтоПомощник = Истина;
		
		ОМ_Сервер.СообщитьПользователю("Если Вам нужно обходить эту конфигурацию - активируйте вот этот флажок",,, "Объект.Отслеживать");
		
	КонецЕсли;
	
КонецПроцедуры
