
Процедура ПередЗаписью(Отказ)
	
	Если Не ОбменДанными.Загрузка Тогда
		
		Структура = Новый Структура(ОМ_Прокси.Получить_СтруктуруПрокси_Строкой());
		ЗаполнитьЗначенияСвойств(Структура, ЭтотОбъект);
			
		Наименование 	 = ОМ_Прокси.Сформировать_ПредставлениеПрокси(Структура);
			
	КонецЕсли;
	
КонецПроцедуры
