
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Элементы.Группа2.Видимость 		= ЗначениеЗаполнено(Запись.Объект);
	
	Если ЗначениеЗаполнено(Запись.Объект) Тогда
		Элементы.ТипОбъекта.Заголовок 	= ОМ_Сервер.СформироватьПредставлениеТипа(ТипЗнч(Запись.Объект));
	КонецЕсли;

КонецПроцедуры
